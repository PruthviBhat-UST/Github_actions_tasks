name: trying pr event trigger

on: 
  pull_request:
    branches:
      - main  
      - master

jobs:
  validate:
    runs-on: ubuntu-latest  
    steps:
     - uses: actions/checkout@v4
     - run: echo "trying pull request"
